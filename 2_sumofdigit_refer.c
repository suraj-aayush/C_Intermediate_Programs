#include <stdio.h>
#include <string.h>
#include <stdlib.h>
void sod(int *,int );

int main(){
    int n,i,s=0;
    printf("enter number ");
    scanf("%d",&n);

    sod(&n,sum);
    printf("%d is sum ",sum);

return 0;
}

void sum(, int)
int r,j, sum=0;
{
   while (j!=0)
   {
       r=
   }
    
}